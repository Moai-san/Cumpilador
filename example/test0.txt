babak main
{
    pancet bool flag = palsu;
    babak number;

    yen ((1 < 1000))
    {
        nyetak ("Variable es verdadera");
        number = FACT(9);
    }
    liyane
    {
        nyetak ("Variable es falsa");
        number = MOD(10,4);
    }

    nyetak ("El numero es:");
    nyetak (number);
}